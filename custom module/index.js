const custom_module = require("./module");            // to import a Coomon JS module

console.log(custom_module);                           // this will print all the objects imported.



console.log(custom_module.sum([2, 3, 4]));            //
