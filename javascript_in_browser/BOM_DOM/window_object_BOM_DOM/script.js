// Note :- [window] is a global object, so window.console.log() is same as console.log()

// DOM :- Docuument  Object Model
// BOM :- Browser Object Moddel

// DOM represents the page contant as Javascript.
document.write("HELLO");

console.log(window);
console.log(document);
console.log(document.body);
document.body.style.background = "BLUE";

// DOM in Javascript can be used to change the CSS.
// It is used to change CSS on a particular action of user (for example :- selecting a option, etc...)